# Plan: Connecteam workforce research and backlog refinement

## Scope and ownership

Execute [spec.md](spec.md) for research and publication only. Preserve the existing active
`specs/002-schedule-calendar-redesign/` state; do not change the shared feature pointer. One root writer
owns this directory, the detailed report and image atlas, the small original-report navigation update,
the Connecteam engineering memory and a roadmap navigation link. Researchers provide read-only evidence.

## Design and reuse

Keep CT-01–90 stable. Link the original comparison rather than duplicating its entire implementation
matrix. Add deeper individual workflows, explicit source caveats, a visual atlas and C1–C7 candidate
contracts. Preserve existing domain transitions, scoped role grants, handover/checklist evidence,
notification outbox, employee identity and Schedule/AI issue ownership. Future frontend changes must
follow existing FSD/public APIs, prepared view models, shared shadcn components, Query server state and
Zustand client drafts; no implementation architecture migration is part of this research.

## Ordered work

1. Inventory live issues and current relevant source; capture concurrent paths and source revision.
2. Refresh official feature guides, inspect representative public screenshots and record uncertainty.
3. Consolidate all capability IDs and prioritize bounded manufacturing adaptations under Lean review.
4. Write proposed acceptance contracts without assuming operating-policy answers or runtime approval.
5. Prepare additive issue sections with a unique marker; re-fetch each issue immediately before edit.
6. Update #3 and #36–53 sequentially, preserving body edits and all other metadata; verify exact appended
   text and unchanged title/labels/state/hierarchy by live API readback.
7. Save publication receipts, validate owned artifacts, make one coherent documentation commit and push.

## Verification and recovery

Validate CT coverage, C/UX acceptance IDs, source references, relative local links, image existence,
Markdown formatting and whitespace. No application build/tests are warranted for a documentation-only
change. Screenshots establish competitor visual evidence, not Vakhta feature QA. Before an uncertain
GitHub retry, read the issue; replace only this change's marked section, never the complete historical
publication snapshot. No new issues, assignees, labels, closure or reassignment is needed.

GitHub is the authority for live issue progress. Receipts record publication, not product delivery.
Normal master push only; integrate remote changes without overwriting or rebasing other work.

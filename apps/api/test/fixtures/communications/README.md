# Synthetic recordings

Half-second black 16x16 H.264 video and mono MP3 silence, generated locally with FFmpeg lavfi.
They contain no employee data and exercise actual-format admission. Tests read the committed bytes;
FFmpeg is not a CI dependency.
